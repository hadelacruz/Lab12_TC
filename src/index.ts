/**
 * LABORATORIO 12 - TEORÍA DE COMPUTACIÓN
 * Programación Funcional con TypeScript
 * 
 * Este programa demuestra el uso de programación funcional
 * con funciones lambda (arrow functions) en TypeScript
 */

import { sortByKey } from './problema2';
import { transpose } from './problema3';
import { removeElements } from './problema4';

export { sortByKey, transpose, removeElements };
